<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Your order</h4>
            </div>
            <div class="modal-body">
                @*<ul class="list-group ">
                    <li class="list-group-item">Тип  : <span class="text-info pull-right ">{{selected.typecar.GroupName}} {{selected.typecar.Name}}</span></li>
                    <li class="list-group-item ">Город  :<span class="text-info pull-right">{{selected.city.Name}}</span></li>
                    <li class="list-group-item">Год выпуска машины <span class="text-info pull-right">{{selected.years}}</span></li>
                    <li class="list-group-item">Сфера использования <span class="text-info pull-right">{{selected.scopeuse.Name}}</span></li>
                    <li class="list-group-item ">Цена:<span class="text-warning pull-right">{{calculation()}}</span></li>
                </ul>*@
                <table class="table table-bordered">
                    <tr>
                        <td>Type :</td>
                        <td>{{selected.typecar.GroupName}} {{selected.typecar.Name}}</td>
                    </tr>
                    <tr>
                        <td>City :</td>
                        <td>{{selected.city.Name}}</td>
                    </tr>
                    <tr >
                        <td >Car manufacture </td>
                        <td>{{selected.years}}</td>
                    </tr>
                    <tr >
                        <td >Scope of use </td>
                        <td>{{selected.scopeuse.Name}}</td>
                    </tr>
                    <tr >
                        <td>Price</td>
                        <td class="text-warning">{{calculation()}} грн.</td>
                    </tr>
                </table>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary"><i class="fa fa-shopping-cart" aria-hidden="true"></i> Заказать</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>